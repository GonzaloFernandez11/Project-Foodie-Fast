import { Button } from "./ui/button";

const MainNav = () => {
    return(
        <Button variant="ghost" className="font-bold hover:text-white hover:bg-pink-600">
            Log In
        </Button>
    )
}

export default MainNav;